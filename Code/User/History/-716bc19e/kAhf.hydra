fn main() -> void {

    let x = 10;
    let y = 3.14;
    let const name = "John";
    let active = true;
    let inactive = false;
    let empty = null;

    let sum = x + y;
    let diff = x - 10;
    let product = 5 * 6;
    let quotient = 20 / 4;
    let remainder = 7 % 3;

    x += 5;
    y -= 2;
    sum *= 3;

    x++;
    y--;

    let function_call_sum = add(x, 5);

    if (function_call_sum == 15) {
        let is_fifteen: bool = true;
    }

    printf("%d", x);
}

fn add(a: i32, b: i32) -> i32 {
    return a + b;
}